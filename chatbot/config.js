// Safe public defaults. This file intentionally contains no API key.
// The browser only talks to this app's own server-side endpoint.

window.CHATBOT_CONFIG = Object.freeze({
  apiEndpoint: '/api/chat',
});
